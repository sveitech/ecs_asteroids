/**
 * An entity is a collection of components. An entity is nothing more than
 * an identifier, pointing to one or more components.
 */
export default class Entity {
  constructor(public id: number) {}
}
